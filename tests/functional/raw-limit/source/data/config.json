{
  "parameters": {
    "db": {
      "endpoint": "%env(string:ENDPOINT)%",
      "#key": "%env(string:KEY)%",
      "databaseId": "%env(string:DATABASE_ID)%"
    },
    "id": 123,
    "name": "row123",
    "containerId": "movie",
    "output": "output-csv",
    "mode": "raw",
    "select": "c.title AS id, c.year",
    "limit": 2
  }
}
